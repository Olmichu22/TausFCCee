# Diagnostics classification

The equal-statistics tau-topology, PID, PFO-efficiency and photon-origin
programs are reproducible freeze builders, but remain tightly coupled to the
2026-09-01 artifact graph. They are therefore preserved as scientific reports
and provenance, not copied into the active runtime path. Future generic
diagnostics should consume explicit manifests and shared definitions before
being added here.
